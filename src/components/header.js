import React from 'react';

//import CSS file

function Header() {
    return (
        <header className="header">
            <h1> Portfolio </h1>
        </header>
    );
}

export default Header;
